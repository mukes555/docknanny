## What and why

<!-- What changes, and the reason. The reason matters more. -->

## Verified by hand

<!-- Permission flows and multi-monitor behaviour cannot be unit tested. Say what
     you actually exercised: how many displays, which edge, which permissions
     granted or denied. Attach a screenshot or recording for anything visual. -->

- [ ] `swiftlint --strict` passes
- [ ] `xcodebuild test` passes
- [ ] Checked on more than one display, or noted why that is not applicable
- [ ] No setting is exposed in the UI that nothing reads

## Private Apple APIs

<!-- Delete if untouched. Otherwise: which symbol, why there is no public
     equivalent, and what the app does when it disappears. -->
